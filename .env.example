# Docker Hub namespace (username or org) — must be lowercase
NAMESPACE=yourdockerhubname

# Repository name (lowercase, matches your GitHub repo lowercased)
REPO=event-finder-website

# Tag to deploy (use 'latest' for main, or a git tag/version)
TAG=latest

